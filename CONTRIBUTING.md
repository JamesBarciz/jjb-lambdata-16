# Contributor's Guide

# Installation

Git clone and navigate to the repo from CLT

```sh
pipenv --python 3.7
pipenv install
pipenv shell
```

Run example script:

```sh
python -m my_lambdata.hello
```